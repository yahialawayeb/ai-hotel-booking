# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_DB=hotel_db_example
DB_PORT=5432

# PgAdmin Configuration
PGADMIN_EMAIL=admin@example.com
PGADMIN_PASSWORD=secure_password
PGADMIN_PORT=5050

# Server Configuration
SERVER_PORT=4000
JWT_SECRET=your_super_secure_secret_key_change_this
# Note: DATABASE_URL is constructed in docker-compose.yml using the variables above

# Client Configuration
CLIENT_PORT=3000
NEXT_PUBLIC_API_URL=http://localhost:4000
# INTERNAL_API_URL is handled internally in docker-compose to point to 'server' service

# Local Development Database connection string (optional, for running outside docker)
DATABASE_URL="postgresql://postgres:changeme@localhost:5432/hotel_db_example?schema=public"
